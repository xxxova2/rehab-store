/**
 * Rehab 3D — React Three Fiber scenes, viewer, mannequin, AR helpers.
 * Phase 0 ships the orchestrator + placeholder. Full garment viewer in Phase 1.
 */

export { Stage } from './stage';
export type { StageProps } from './stage';
