'use client';

import Spline from '@splinetool/react-spline';
import type { SplineProps } from '@splinetool/react-spline';

export function SplineScene(props: SplineProps) {
  return <Spline {...props} />;
}
